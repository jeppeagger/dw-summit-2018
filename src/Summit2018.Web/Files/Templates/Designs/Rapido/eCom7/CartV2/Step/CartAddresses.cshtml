@* This template is not meant for rendering the cart.
    Instead, what it does is making it possible to make a
    layout template for a feed containing addresses for the 
    checkout flow. To use it, chose this template in the cart module
    where the feed should be generated.  *@